package design.design18_memento;

public class text {
    /**
     * 备忘录模式：
     *保存一个对象的某个状态，以便在适当的时候恢复对象
     *原始类A，A中有各种属性，A可以决定需要备份的属性，
     *忘录类B是用来存储A的一些内部状态，
     *类C用来存储备忘录的，且只能存储，不能修改等操作
     */
}
